// Navbar.js
import React from "react";
import styles from "./Navbar.module.css"; // Import CSS module for styling

const Navbar = () => {
  return (
    <nav
      className={`${styles.navbar} navbar navbar-expand-lg bg-dark navbar-dark p-3 p-lg-0`}
    >
      {/* Navbar content */}
    </nav>
  );
};

export default Navbar;
